<UserControl
    x:Class="TtsClient.Views.MultiFileUploadPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:behaviors="clr-namespace:TtsClient.Behaviors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:TtsClient.ViewModels"
    d:DataContext="{d:DesignInstance viewModels:MultiFileUploadPageViewModel}"
    d:DesignHeight="300"
    d:DesignWidth="300"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <ListBox
            Grid.Row="1"
            Grid.Column="0"
            ItemsSource="{Binding TextFiles}"
            SelectedItem="{Binding SelectedItem}">

            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type viewModels:FileListItem}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding FileInfo.Name}" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>

            <b:Interaction.Behaviors>
                <behaviors:DropFileBehavior />
            </b:Interaction.Behaviors>
        </ListBox>

        <TextBox
            Grid.Row="1"
            Grid.Column="1"
            AcceptsReturn="True"
            AcceptsTab="True"
            Text="{Binding Text}" />

        <StackPanel
            Grid.Row="2"
            Grid.Column="0"
            Grid.ColumnSpan="2"
            Margin="5"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button Content="Registration" />
        </StackPanel>
    </Grid>
</UserControl>