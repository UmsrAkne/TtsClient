<UserControl
    x:Class="TtsClient.Views.ExplorerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:TtsClient.Models"
    xmlns:viewModels="clr-namespace:TtsClient.ViewModels"
    d:DataContext="{d:DesignInstance viewModels:ExplorerPageViewModel}"
    d:DesignHeight="720"
    d:DesignWidth="1200"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="{StaticResource CommonFontSize}" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.5*" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <ListBox
            Grid.Row="0"
            Grid.RowSpan="2"
            Grid.Column="0"
            ItemsSource="{Binding SpeechEntries}"
            SelectedItem="{Binding CurrentEntry}">

            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type models:SpeechEntry}">
                    <DataTemplate.Resources>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontSize" Value="{StaticResource CommonFontSize}" />
                        </Style>
                    </DataTemplate.Resources>

                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding DisplayName}" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Grid
            Grid.Row="0"
            Grid.RowSpan="2"
            Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>

            <TextBox
                AcceptsReturn="True"
                FontSize="{StaticResource CommonFontSize}"
                IsReadOnly="True"
                Text="{Binding CurrentEntry.Contents}" />

            <Border Grid.Row="1">
                <ListBox ItemsSource="{Binding CurrentEntry.AudioFiles}" SelectedItem="{Binding CurrentEntry.SelectedAudio}">
                    <ListBox.ItemTemplate>
                        <DataTemplate DataType="{x:Type models:AudioFileEntry}">
                            <ContentControl>
                                <b:Interaction.Triggers>
                                    <b:EventTrigger EventName="MouseDoubleClick">
                                        <b:InvokeCommandAction Command="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=ListBox}, Path=DataContext.PlayAudioCommand}" />
                                    </b:EventTrigger>
                                </b:Interaction.Triggers>
                                <TextBlock Text="{Binding AudioRelativePath}" />
                            </ContentControl>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>
        </Grid>
    </Grid>
</UserControl>